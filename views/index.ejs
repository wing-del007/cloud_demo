<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Manager</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      background: #8B6F47;
      background-image:
        radial-gradient(circle at 20% 80%, #A9824F 0%, transparent 20%),
        radial-gradient(circle at 80% 20%, #7A5D3A 0%, transparent 20%),
        linear-gradient(135deg, #9C7A50 25%, transparent 25%) 70px 0,
        linear-gradient(225deg, #8B6F47 25%, transparent 25%) 70px 0,
        linear-gradient(135deg, transparent 75%, #7A5D3A 75%),
        linear-gradient(225deg, transparent 75%, #A9824F 75%);
      background-size: 140px 140px, 180px 180px, 100px 100px, 100px 100px, 100px 100px, 100px 100px;
      background-blend-mode: multiply;
      min-height: 100vh;
    }
  </style>
</head>
<body>
  <header class="bg-amber-800/95 backdrop-blur shadow-lg border-b-4 border-amber-900">
    <div class="max-w-7xl mx-auto px-6 py-5 flex justify-between items-center">
      <div class="flex items-center gap-5">
        <svg class="w-12 h-12 text-amber-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332 .477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332 .477-4.5 1.253"/>
        </svg>
        <h1 class="text-4xl font-bold text-amber-100 tracking-wider">Book Manager</h1>
      </div>
      <a href="/logout">
        <button class="px-8 py-3 bg-red-800 hover:bg-red-900 text-white rounded-full font-medium transition shadow-lg">
          Logout
        </button>
      </a>
    </div>
  </header>

  <div class="bg-white min-h-screen">
    <div class="max-w-7xl mx-auto px-6 pt-12">
      <div class="flex flex-col lg:flex-row justify-between items-center gap-6 mb-12">
        <form method="GET" action="/" class="w-full lg:w-auto">
          <div class="flex gap-4">
            <input type="text" name="q" placeholder="Search by title, author, ID..." value="<%= search || '' %>" 
              class="px-8 py-4 rounded-full border-2 border-amber-700 text-lg w-full lg:w-96 focus:outline-none focus:ring-4 focus:ring-amber-300">
            <button type="submit" class="px-10 py-4 bg-amber-700 hover:bg-amber-800 text-white font-bold rounded-full shadow-lg transition">
              Search
            </button>
            <a href="/" class="px-10 py-4 bg-amber-700 hover:bg-amber-800 text-white font-bold rounded-full shadow-lg transition pt-[19px]"">Clear</a>
          </div>
        </form>

        <% if (session.user && session.user.role === 'admin') { %>
          <a href="/add" class="px-12 py-5 bg-green-700 hover:bg-green-800 text-white text-xl font-bold rounded-full shadow-xl transition transform hover:scale-105">
            + Add New Book
          </a>
        <% } %>
      </div>

      <div class="space-y-12">
        <% books.forEach(book => { %>
          <div class="bg-amber-50/80 rounded-2xl shadow-xl overflow-hidden hover:shadow-2xl transition">
            <div class="p-8 md:p-12 flex flex-col md:flex-row items-center gap-10">
              
              <div class="flex-shrink-0">
                <div class="w-40 h-56 md:w-48 md:h-64 bg-gradient-to-br from-amber-100 to-amber-300 rounded-lg shadow-2xl border-8 border-amber-900 relative overflow-hidden">
                  <div class="absolute inset-y-0 left-8 w-2 bg-amber-900"></div>
                  <div class="absolute inset-y-0 left-10 w-1 bg-amber-700"></div>
                  <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/30 to-transparent"></div>
                  <div class="flex items-center justify-center h-full">
                    <span class="text-amber-900/30 text-5xl font-bold transform -rotate-90 tracking-widest">BOOK</span>
                  </div>
                </div>
              </div>

              <div class="flex-1 text-center md:text-left">
                <h2 class="text-4xl font-bold text-amber-900 mb-4">
                  [<%= book.bookId %>] <%= book.title %>
                </h2>
                <p class="text-2xl text-amber-800 mb-8">Author: <%= book.author || 'Unknown' %></p>

                <div class="flex justify-center md:justify-start items-center gap-12">
                  <a href="/view/<%= book._id %>" class="px-12 py-4 bg-blue-700 hover:bg-blue-800 text-white font-bold rounded-full shadow-lg transition">
                    View
                  </a>

                  <% if (session.user && session.user.role === 'admin') { %>
                    <a href="/edit/<%= book._id %>" title="Edit" class="group">
                      <svg class="w-12 h-12 text-gray-700 group-hover:text-gray-900 transition transform hover:scale-110" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                      </svg>
                    </a>
                    <form method="POST" action="/delete/<%= book._id %>" style="display:inline">
                      <button type="submit" onclick="return confirm('Are you sure to delete?')" title="Delete" class="group">
                        <svg class="w-12 h-12 text-red-700 group-hover:text-red-900 transition transform hover:scale-110" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
                        </svg>
                      </button>
                    </form>
                  <% } %>
                </div>
              </div>
            </div>
            <div class="mt-10 border-b-2 border-dotted border-gray-300"></div>
          </div>
        <% }) %>

        <% if (books.length === 0) { %>
          <div class="text-center py-32">
            <p class="text-4xl font-bold text-gray-800">No books yet</p>
            <% if (session.user && session.user.role === 'admin') { %>
              <a href="/add" class="inline-block mt-10 text-3xl text-white bg-green-700 px-16 py-6 hover:bg-green-800 rounded-full transition shadow-xl">
                Add the first one now!
              </a>
            <% } %>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>
